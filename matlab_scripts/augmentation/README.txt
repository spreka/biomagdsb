Augmentaiton

Usage:
function augmentTrainingSet(inFolder, outFolder, styleData)

inFolder: input (images/masks) folder
outFolder: creates (images/masks) the augmented data there
styleData: 1-if styles (no serious augmentaiton) 0-otherwise (different colors shifts applied too)


